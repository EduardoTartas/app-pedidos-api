apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: app-pedidos-api
  name: app-pedidos-api
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: app-pedidos-api
  template:
    metadata:
      labels:
        io.kompose.service: app-pedidos-api
    spec:
      restartPolicy: Always
      
      securityContext:
        fsGroup: 1000
      
      containers:
        - name: app-pedidos-api
          image: <seu-usuario>/app_pedidos_api:latest
          imagePullPolicy: Always
          
          ports:
            - containerPort: 5020
              protocol: TCP
          
          envFrom:
            - configMapRef:
                name: pedidos-env
          
          resources: {}

          # Liveness Probe - verifica se o container está vivo
          livenessProbe:
            httpGet:
              path: /
              port: 5020
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          
          # Readiness Probe - verifica se o container está pronto para receber tráfego
          readinessProbe:
            httpGet:
              path: /
              port: 5020
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1
          
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
---

apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: app-pedidos-api
  name: pedidos-api
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 5020
  selector:
    io.kompose.service: app-pedidos-api
