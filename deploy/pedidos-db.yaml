apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: pedidos-mongodb
  name: vol-pedidos-mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
---

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: pedidos-mongodb
  name: pedidos-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: pedidos-mongodb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: pedidos-mongodb
    spec:
      containers:
        - image: mongo:8
          livenessProbe:
            exec:
              command:
                - mongosh
                - --eval
                - db.adminCommand('ping')
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
          name: pedidos-mongodb
          ports:
            - containerPort: 27017
              protocol: TCP
          envFrom:
            - configMapRef:
                name: pedidos-env
          volumeMounts:
            - mountPath: /data/db
              name: vol-pedidos-mongodb
      restartPolicy: Always
      volumes:
        - name: vol-pedidos-mongodb
          persistentVolumeClaim:
            claimName: vol-pedidos-mongodb
---

apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: pedidos-mongodb
  name: pedidos-mongodb
spec:
  type: ClusterIP
  ports:
    - name: "27017"
      port: 27017
      targetPort: 27017
  selector:
    io.kompose.service: pedidos-mongodb
